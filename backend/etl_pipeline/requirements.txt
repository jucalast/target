# Dependências do módulo ETL Pipeline
# Inclui as dependências compartilhadas + dependências específicas de ETL
-r ../shared/requirements.txt

# Processamento de dados
pandas>=2.0.0,<3.0.0
pyarrow>=12.0.0,<13.0.0  # Para cache em parquet
requests>=2.31.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0  # Para processamento de dados

# Integração com fontes de dados
sidrapy>=0.1.4,<0.2.0  # Cliente para a API SIDRA/IBGE
pytrends>=4.9.0,<5.0.0  # Cliente para a API do Google Trends
tenacity>=8.2.3,<9.0.0  # Para retry em chamadas de API
python-rapidjson>=1.10.0,<2.0.0  # Para processamento JSON rápido

# Dependências para testes
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.20.0,<0.21.0
httpx>=0.24.0,<0.25.0  # Cliente HTTP assíncrono para testes
