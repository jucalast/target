# Dependências do módulo API Gateway
# As dependências compartilhadas são instaladas pelo Dockerfile

# Dependências para testes
pytest>=7.0.0,<8.0.0 
pytest-asyncio>=0.20.0,<0.21.0
httpx>=0.24.0,<0.25.0  # Cliente HTTP assíncrono para testes
