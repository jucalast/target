# Dependências do módulo NLP Processor
# Inclui as dependências compartilhadas + dependências específicas de NLP
-r ../shared/requirements.txt

# Processamento de linguagem natural
spacy[transformers]>=3.6.0,<3.8.0
sentence-transformers>=2.2.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0  # Para TF-IDF e LDA

# Dependências para testes
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.20.0,<0.21.0
httpx>=0.24.0,<0.25.0  # Cliente HTTP assíncrono para testes
