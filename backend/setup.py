from setuptools import setup, find_packages

setup(
    name="target-backend",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
    ],
    python_requires=">=3.8",
)
